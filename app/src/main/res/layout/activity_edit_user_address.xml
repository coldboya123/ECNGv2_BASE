<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_2"
    tools:context=".View.User.SettingAccount.Address.EditUserAddressActivity">
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar_user_address_edit"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            android:background="@drawable/appbar_background"
            app:layout_collapseMode="pin"
            android:layout_gravity="bottom">
            <Button
                android:id="@+id/user_address_edit_btn_back"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Chỉnh sửa địa chỉ"
                android:textColor="@color/white"
                android:background="@null"
                android:drawableLeft="@drawable/ic_back_shop"
                android:layout_marginEnd="10dp"
                android:drawablePadding="10dp"
                android:textAllCaps="false"
                android:textSize="18sp"/>
        </androidx.appcompat.widget.Toolbar>
    </com.google.android.material.appbar.AppBarLayout>
    <include
        app:layout_constraintTop_toBottomOf="@id/appbar_user_address_edit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        layout="@layout/user_address_layout"/>
</androidx.constraintlayout.widget.ConstraintLayout>